vertical orientation -> try rotate

make sure it works on OSX
make sure it works on Windows

what about icons? insert base64 PNGs and JPGs?

improve README and PROGRAMMING

________________________________________________________________ v0.1

improve command line interface? (getopts)

translatable strings (gettext)

parseDIR (depth based and then time based)

GRAMPS file format parser
GEDCOM file format parser

optimise both-subtree at creation time

________________________________________________________________ v0.2


take a look at libpng, then maybe implement colormap
signature generator: 500x500 SVG (PNG if possible), average distribution, grayscale

add option curvedConnectors ?

add ability to override options in CSV? (with X flag)
  -> easy to implement, but bad practice (weakening conffile devalues parserDIR etc)
SOLUTION: implement generatorCONF (creates default conf if no csv as input)


________________________________________________________________ v0.3


label backgrounds... maybe use font renderer? (freetype)

base GUI on csveditor (java)?
